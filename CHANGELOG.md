# 변경 이력

## 미출시

- 삼각화된 규칙 격자의 닫힌 합성 얼굴에서 서로 분리된 폐루프 세 개를 확인하며 원래 쿼드 연결을 복원하는 제한적 경로를 추가했습니다. 일반 캐릭터의 새 얼굴·관절 루프 생성은 아직 미지원입니다.
- 격자 결과의 bowtie·가이드 엣지 경로와 양방향 표면 표본 오차를 검사하고, 종횡비 20 초과 결과를 적용하지 않도록 했습니다. 폐루프 판정에서 중심 정점을 여러 번 공유하는 잘못된 경로도 거부합니다.
- worker 입력의 큰 기하 배열을 버전·지문·크기·SHA-256 검사가 있는 바이너리 버퍼로 분리했습니다. 출력은 계속 JSON입니다.
- 비다양체 원본의 큰 메시 경로가 격자 배치 실패 뒤 임시 프록시 처리로 진행하도록 수정했습니다. 임모탈 입력에서 기존 결과의 최대 종횡비 3584.18을 확인했고 새 품질 검사에서는 결과 적용을 거부합니다.
- 루프·패치 배치와 정수 격자 기반의 새 엔진 설계, 데이터 계약, 구현 순서와 품질 합격 기준을 문서화했습니다.
- 연결된 공평면 사각 차트의 공유 정수 분할과 열린 원통형 튜브의 주기 링 격자를 추가했습니다. 큐브 384쿼드와 원통 128쿼드 구조를 격리 Blender에서 확인했습니다.
- `AUTO`·`STRUCTURED`·`LEGACY` 생성 방식을 추가했습니다. 미지원 형상의 실험 엔진 사용을 경고하고 필수 가이드를 보존할 수 없으면 중단합니다.
- 가이드의 `LOOP`·`STRIP`·`DIRECTION` 종류와 스플라인 닫힘 여부를 Blender 입력과 worker 계약에 전달합니다. 원통 단면의 LOOP와 길이 방향 STRIP을 실제 출력 링·엣지 열로 고정합니다.
- 입력 11,094면의 세분화 큐브를 프록시 축소 없이 격자 경로로 처리했습니다. 원본 면과 결과 격자를 검증했습니다.
- 기존 실험 엔진이 균일 격자·캐릭터 부위별 루프 요구를 충족하지 못한다는 점을 README와 검증 문서에 명시했습니다.
- 대칭 평면에 점으로만 닿는 조각을 잘못 용접해 bowtie 연결을 만들던 경우를 수정했습니다.
- 정렬 과정에서 고정된 조각에 불필요한 표면 매핑을 시도하던 오류와 작은 유효 면을 퇴화 면으로 오판정하던 면적 기준을 수정했습니다.
- 큰 입력 worker의 실행·취소·원본 보존 및 특징선 정점 보호 검사를 추가했습니다.
- 큰 입력에서 기존 엔진 상한을 넘는 경우만 격리된 background Blender worker로 우회해 임시 Decimate 프록시를 만들고, 자체 쿼드 엔진과 원본 BVH 기준 검증을 거쳐 결과를 적용하도록 수정했습니다.
- 최종 쿼드 결과는 자체 엔진이 만들지만 큰 입력 프록시 생성에는 Blender Decimate를 사용한다는 점을 명확히 했습니다.
- 목표 쿼드 수와 상용 품질은 보장 대상이 아니며, 실패·취소·입력 변경 시 원본을 보존하고 결과 적용을 중단한다는 설명을 보강했습니다.

## 0.3.0 — 2026-09-19

- 첫 공개 릴리스와 Blender Extensions 원격 저장소를 구성했습니다. 새 릴리스의 검증·ZIP 게시·Pages 업데이트 인덱스 생성을 자동화했습니다.
- 누적 QEM과 원본 표면 거리 검사를 적용한 엣지 축소·분할로 폴리곤 감소 및 목표 수 피드백 제어를 구현했습니다.
- 형상 편차가 크면 해상도를 보정하고 기준을 넘는 결과 적용을 거부합니다.
- 로컬 원점의 양의 영역을 기준으로 X/Y/Z 절단·미러·경계 연결을 구현했습니다.
- 특징선·곡률·가이드의 4방향장을 계산하고 패치 선택과 내부 정점 정렬에 적용합니다.
- 밀도 페인트를 실제 엔진에 전달하고, 원본 표면 재투영과 표면 편차·대칭 오차·방향 정렬 점수를 추가했습니다.
- 형상 제약으로 목표 개수에 도달하지 못한 경우에도 현재 면 수와 표면 편차 기준을 유지하며 밀도 재분배를 수행합니다.
- 고밀도 영역의 허용 표면 편차를 좁혀 세부 면을 보존하고, 거의 같은 축소 비용의 플랫폼별 정렬 차이를 줄였습니다.
- 위상과 특징선 제약으로 목표 수가 정확히 실현되지 않으면 실제 개수와 오차를 보고합니다.

## 0.2.0 — 2026-09-19

- 독립 Python 실험 엔진으로 특징선을 보존하는 쿼드 메시 생성 경로를 추가했습니다.
- 인접 삼각형의 형상과 가이드 방향을 평가하고 공유 엣지 중점과 면 중심으로 쿼드를 생성합니다.
- 목표 수에 가까운 균일 분할 단계 선택과 실제 쿼드 수·품질 보고를 추가했습니다.
- 성공한 결과를 별도 오브젝트로 만들며 실패·취소 시 원본 데이터를 보존합니다.
- 폴리곤 감소, 정확한 목표 개수, 강제 대칭, 적응형 밀도와 전역 엣지 흐름 최적화는 아직 지원하지 않습니다.

## 0.1.0 — 2026-09-19

- Blender Extension 개발 기반과 독립 엔진 입력 경계를 구성했습니다.
- 목표 쿼드 수, 대칭 축, 하드 엣지, 가이드 커브, 밀도 설정을 준비했습니다.
- 메시 분석과 밀도 속성 준비 기능을 추가했습니다.
- macOS와 Windows 전용 개발 프로필 실행기, 로컬 패키징 및 CI 검증 구성을 추가했습니다.
- 리토폴로지 엔진과 쿼드 메시 생성은 후속 구현 대상입니다.
