# 변경 이력

## 0.6.0 — 2026-09-23

- 패널에 **클릭으로 링 가이드 추가**를 넣었습니다(`addon/ring_guide.py`, `addon/ring_geometry.py`). 메시 표면을 클릭하면 클릭 점을 지나는 평면들 가운데 단면 둘레가 짧고, 평면을 앞뒤로 옮겨도 둘레가 절반 이상 유지되며, 루프를 따라 면 법선이 절단 축과 수직인 방향을 반구 전역 탐색과 단계적 정밀화로 골라 실제 단면 커브(`REMESH_GUIDE_ring`)를 만듭니다. 두께를 먼저 재서 중심을 잡는 방식은 턱 아래에서 머리 속으로 들어갔고, 접평면 안 후보만 보는 방식은 목에서 후보가 전부 기각돼 폐기했습니다. 단면 계산은 numpy 로 벡터화해 12,462면 캐릭터에서 클릭 5회에 약 2초입니다.
- 링 추가 중 Ctrl+Z·Backspace 로 이 세션에서 만든 마지막 링만 취소하고(전역 언도는 모달 중 삼킵니다), 헤더에 추가된 개수를 표시합니다. 패널 목록의 X 버튼으로 개별 삭제, 쓰레기통 버튼으로 전체 삭제할 수 있습니다.
- 사용자 캐릭터에 목 링 둘을 두고 목표 3,000 으로 돌리면 "비다양체 엣지 3" 으로 중단되던 문제를 고쳤습니다. 원인은 셋이었습니다. 같은 축 위에 가까이 둔 링 둘을 중복으로 합쳐 하나를 버렸고(중심 거리만 비교), 대칭면 옆에 구멍 메우기가 남긴 얇은 웹의 내부 정점이 평면 위에 있어 미러 용접이 면 4개짜리 엣지를 만들었으며, 대칭면 루프에 붙은 작은 구멍을 `holes_fill` 이 평면을 가로질러 메워 목 안쪽에 웹(표면 오차 0.127)을 만들었습니다. 중복 판정은 축 방향 간격이 띠 폭 안일 때만 하고, 미러 전에 평면 위 내부 정점을 용접 임계값의 두 배만큼 띄우며, 평면 위 엣지는 메우지 않고 평면 밖으로 튄 체인만 n각형으로 닫습니다.
- 접합 검증을 강화했습니다. 열린 호는 호 길이 비율도 비교하고, 접합으로 생긴 엣지가 링 엣지의 4배를 넘거나 겹친 면을 만들면 그 접합을 되돌리고 해당 루프를 빼서 다시 깝니다. 같은 쪽의 조각난 호는 끝점 거리 3.5엣지 안에서 이어 붙입니다. 이웃 링과의 축 방향 간격이 출력 쿼드 3개보다 좁으면 경고합니다 — 이 캐릭터의 목(높이 약 0.12)에는 목표 2,000 에서 링 하나만 들어가고, 둘을 두려면 목표 6,000 이상에서 목 위·아래 끝에 배치해야 합니다.
- 가이드 오브젝트에 축 오프셋·둘레 비율·상태 속성을 두고, 오프셋을 바꾸면 그 위치의 단면을 다시 잘라 커브를 갱신합니다. 패널은 가이드 목록과 비율을 보여 주고 0.5 미만이면 위치 조정을 권합니다. **링 가이드 다시 검사**로 목표 쿼드 수 변경 뒤 재계산할 수 있습니다.
- 좀비 캐릭터에서 위팔·팔뚝·허벅지·정강이·목 앞을 클릭해 만든 가이드 5개가 모두 "사용 가능"(둘레 비율 0.89~0.99)으로 나오고, `QUADRIFLOW` 목표 2,000·X 대칭에서 다섯 링이 모두 접합됐습니다(목 19·17, 위팔 7·8, 팔뚝 10·10, 허벅지 14·11, 정강이 11·10, 1,882쿼드, 최대 종횡비 3.86). 허벅지 축은 31° 기울어 잡혀 QuadriFlow 시드에 따라 한쪽 링이 대칭면에 잘려 제외되는 실행도 있었습니다.

## 0.5.1 — 2026-09-23

- 0.5.0 의 릴리스 검사가 `tests/blender_acceptance.py` 에서 멈춰 태그 `v0.5.0` 은 릴리스 자산 없이 남았습니다. 이 테스트는 균일 토러스를 실험 엔진으로 돌리면 종횡비 31.78 로 거부되기를 기대했는데, 0.5.0 의 주곡률 방향 앵커 뒤로 같은 입력이 종횡비 15.4 로 통과합니다. 테스트는 거부와 적용 두 결과를 모두 받아 원본 보존과 종횡비 상한(20)을 확인하도록 고쳤습니다. 코드 변경은 없습니다.

## 0.5.0 — 2026-09-23

- 팔·다리 원통에 나선 격자가 생기던 문제를 두 경로에서 고쳤습니다. 원통은 가우스 곡률이 0 이고 앵커가 없어 방향장 회전각이 미결정이라 임의 각도로 굳는 것이 원인이었습니다(실측 2026-09-22, 구+원통 합성 입력의 QuadriFlow 목표 1,500: 팔 영역 닫힌 링 0개, 열린 나선 루프 34개).
- 실험 엔진의 4방향장(`addon/field.py`)은 곡률 크기만 쓰던 앵커를 이면각 기반 곡률 텐서의 주곡률 방향 앵커로 바꿨습니다. 가중치는 상대 비등방성과 절대 크기로 정해 구·평면에서는 0 에 가깝습니다. 비틀린 삼각화 원통(twist 0.5)의 중간 높이 정렬이 최소 0.956 → 0.996 으로 올랐고, 정규 삼각화 원통은 전후 모두 1.000 입니다.
- QuadriFlow 경로가 닫힌 `LOOP` 가이드를 절단 링으로 받습니다(`addon/ring_cut.py`). 루프 평면 ±(출력 엣지 절반)의 띠를 지워 경계를 만들고 `use_preserve_boundary` 로 돌리면 경계가 앵커가 되어 링이 축을 따라 전파됩니다. QuadriFlow 가 닫힌 경계 링에 씌우는 평면 캡을 지우고 양쪽 링을 최소 비용 DP 브리지로 이어, 정점 수 차이만큼만 전이 삼각형이 남습니다(실측 20·18정점 → 삼각형 2개, 접합부 밸런스 4 이외 1~2개). 링 정점을 눌러 골을 만드는 sharp 방식은 효과가 없었고, 단순 분할은 QuadriFlow 캡이 겹쳐 접합부에 3·5가 폴이 20개 넘게 생겨 채택하지 않았습니다.
- `AUTO` 는 격자 경로가 `LOOP` 를 보존하지 못하면 실험 엔진으로 내려가지 않고 QuadriFlow 절단 링을 시도하며, 그것도 실패하면 중단합니다. `QUADRIFLOW` 모드는 `LOOP` 를 받고 `STRIP` 만 거절합니다. 결과 경고에 접합 링 정점 수·전이 삼각형 수와, 접합부 양쪽으로 닫힌 평행 링이 몇 개 이어지는지(`ring_propagation`) 보고합니다.
- 사용자 캐릭터(좀비, 12,462면, `LOOP` 4개, X 대칭)로 확인한 세 가지를 고쳤습니다. 손으로 그린 원이 실제 단면보다 작아도 링을 찾도록 근접 판정을 가이드 폴리라인 거리에서 링 중심 거리로 바꾸고, 절단 띠는 가이드에 가장 가까운 연결 조각 하나만 지우며 실제 표면 링 반지름을 측정해 접합에 씁니다. 발목처럼 단면이 급변해 양쪽 링 정점 수가 크게 다른 경우(10·24)도 전이 삼각형으로 잇습니다. 접합 불가 시 Blender 일반 브리지로 대체하던 폴백은 비다양체 엣지를 만들어 제거했고, 이유(한쪽 링 열림·정점 수 차이)를 오류에 표시합니다. 결과는 1,880쿼드, 쿼드 98.6%, 대칭 오차 0 이며 양팔·허벅지·정강이에 링 루프가 생겼습니다(`dist/ZombieRingReview-*.png`).
- 절단면 양쪽 둘레가 절반 이상 어긋나는 루프(발목 평면이 발등을, 손목 평면이 손을 함께 지나는 경우)는 자르지 않고 위치를 옮기라는 경고만 남깁니다. QuadriFlow 뒤 접합에 실패한 루프는 그 루프만 빼고 같은 단계를 다시 깔아 나머지 링을 살립니다. 좀비 목에 대칭면을 가로지르는 `LOOP` 를 두어 열린 호 접합(18·18정점, 삼각형 0)과 목 둘레 수평 링을 확인했습니다(`dist/ZombieRingReview-neck*.png`).
- 격리 Blender 5.2 워커 `tests/blender_quadriflow_ring_worker.py`: 구+팔 둘 합성 입력에 +x 팔 둘레 `LOOP` 하나 → 비대칭 1,021쿼드(절단 링 18·18, 삼각형 0, 팔 쪽 닫힌 평행 링 4/4), X 대칭 1,794쿼드(대칭 오차 0, 링 23·18, 전이 삼각형 1, 양쪽 팔 링 전파), 루프를 −x 팔에만 그린 X 대칭 1,680쿼드(양의 쪽으로 미러해 처리), `AUTO` 도 같은 경로로 이어짐, 원본 보존. QuadriFlow 시드 검사에 미세 엣지 비율 선별을, 구멍 수리에 전형 엣지 5% 안의 경계 정점 용접을 추가했습니다. 대칭면을 가로지르는 `LOOP` 는 코드 경로만 있고 검증하지 못했습니다.

## 0.4.2 — 2026-09-21

- 10만면 실제 캐릭터 입력에서 QuadriFlow 경로가 실패하던 두 원인을 고쳤습니다. QuadriFlow 가 구멍 가장자리에 남긴 2mm 남짓한 슬리버 면(최대 종횡비 22.2)은 슬리버 주변 정점만 골라 다시 펴서 7.98 로 내렸고, 구멍 가장자리의 겹친 정점(크랙) 때문에 메워지지 않던 3엣지 구멍은 경계 정점을 먼저 용접하고 여러 번 메우도록 했습니다. 결과에 열린 경계가 남으면 적용하지 않습니다.
- 큰 메시 프록시 경로는 면적이 없는 면을 미리 제외하고, QuadriFlow 경로를 건너뛴 사유를 프록시 경로 오류 메시지에 함께 보고합니다. 이전에는 "N번 면은 면적이 없어 퇴화되었습니다" 만 보여 원인을 알 수 없었습니다.
- 격리 Blender 5.2 검증: 102,036면·204조각·비다양체 엣지 791개인 캐릭터를 `AUTO` 목표 6,000 에서 5,997쿼드(비대칭, 종횡비 7.98, 5.8초)와 5,382쿼드(X 대칭, 종횡비 6.06, 대칭 오차 0, 6.8초)로 처리했습니다.

## 0.4.1 — 2026-09-21

- 경계 엣지 비율이 20%를 넘는 열린 판 형상은 QuadriFlow 경로에서 제외합니다. 부피가 없는 평면을 복셀 리메시하면 닫힌 베개 형상이 되어, 0.4.0의 `AUTO`가 단일 평면 목표 64에서 정확한 64쿼드 대신 68쿼드의 닫힌 껍질을 만들었습니다. 이런 입력은 이전처럼 실험 엔진이 처리합니다.
- 취소 시 worker 가 먼저 죽어도 같은 프로세스 그룹에 남은 QuadriFlow 프로세스를 거둡니다.

## 0.4.0 — 2026-09-21

- 격자 전략이 다루지 못하는 일반 형상에 복셀 리메시와 QuadriFlow 로 새 쿼드 와이어를 까는 범용 경로를 추가했습니다. 복셀 리메시(촘촘히) → 파편 제거 → Decimate → 매니폴드 수리 → 자식 Blender 프로세스의 QuadriFlow(시드·시간 제한 사다리) → 작은 구멍 쿼드 메움 → 원본 표면 투영 슈링크랩과 릴랙스 순서이며, 결과는 원본 표면 거리·비다양체·종횡비 20·대칭 검사를 거칩니다. 3DModelMaker-Blender 의 리토폴로지 실측 노하우(미세 엣지 늘리기, 초과 면만 삭제, 리메시 결과 재리메시 금지, 순차 시드 재시도)를 옮겼습니다.
- 대칭은 QuadriFlow 자체 대칭 모드 대신 양의 반쪽을 절단해 경계를 보존한 채 깔고 미러 모디파이어로 용접합니다. QuadriFlow 대칭 모드는 모든 시드에서 대칭면을 따라 84~115엣지 구멍을 남겼고, 반쪽 방식은 구멍 없이 대칭 오차 0 을 냈습니다. 여러 축의 모서리는 평면 쌍마다 정점 하나만 두 평면에 붙여 비다양체를 막습니다.
- `QUADRIFLOW` 생성 방식을 추가했습니다. `AUTO` 는 격자 → QuadriFlow → 실험 엔진 순서로 시도하며, 밀도 속성이나 `DIRECTION` 가이드가 있으면 이를 반영하는 실험 엔진을 유지합니다. 큰 입력 경로도 프록시 축소 전에 QuadriFlow 를 시도합니다. `AUTO`·`QUADRIFLOW` worker 는 bpy 가 필요해 Blender 바이너리로 실행합니다.
- 격리 Blender 5.2 검증: Suzanne 500면 → 목표 2,000에 2,158쿼드(X 대칭, 최대 종횡비 3.82, 2.4초), 비대칭 2,194쿼드(종횡비 2.87). ico sphere 20,480면 XYZ 대칭 → 1,176쿼드(종횡비 1.33, 표면 오차 0.0035, 2.6초, 이전에는 종횡비 491.97 로 거부). 균일 토러스 768면 → 167쿼드(이전 실험 엔진은 종횡비 31.78 로 거부). 텍스처 UV·베이크는 옮기지 않았습니다.
- 취소 시 worker 와 QuadriFlow 자식 프로세스를 프로세스 그룹째 종료하고, worker 는 SIGTERM 을 협조적 취소로 바꿔 자식을 정리합니다. QuadriFlow 중간 `.blend` 는 부모가 지우는 작업 폴더에 둡니다. 경로 전체에 240초 시간 예산과 입력 400,000면 상한을 두어 초과하면 다음 경로로 넘깁니다. 격리 Blender 5.2 에서 취소 뒤 자식 프로세스와 작업 폴더가 남지 않음을 확인했습니다.
- 중간 정점이 평면 링을 이루지 않는 독립 불규칙 삼각 관을 단면 교차로 재격자화합니다. 두 평면 열린 경계와 하나의 별모양 단면이 이어지는 입력으로 한정하고, `LOOP` 관절 링과 `STRIP` 길이 열을 출력 엣지에 배치합니다. 합성 입력의 격리 Blender 5.2 worker에서 256쿼드와 원본 보존을 확인했습니다.
- 눈·코·입·귀가 돌출된 합성 단일 얼굴에서 앞·양측 여섯 바깥 `LOOP`를 검증했습니다. 격리 Blender 5.2 worker의 목표 5,000에서 5,130쿼드, 최대 면 종횡비 4.900, 연속 폐루프 여섯 개와 원본 보존을 확인했습니다. 눈꺼풀·입술 띠, 개구부나 일반 캐릭터 부위 인식을 구현한 것은 아닙니다.
- 축에 정렬된 원형 단면의 단일 닫힌 T형 합성 입력에 몸통 링 둘·팔 링 하나·팔 방향 `STRIP` 하나를 요구하는 제한적 생성기를 추가했습니다. 격리 Blender 5.2 worker에서 1,072쿼드, 단일 닫힌 컴포넌트, 세 폐루프·한 방향선과 원본 보존을 확인했습니다. 둥근 겨드랑이 전이의 해법은 이 형상과 가이드 조건에 한정됩니다.
- 격자 생성기들의 구현 검증은 합성 메시로만 진행했습니다. 위 새 생성기들의 품질 수치는 기존 임모탈 메시나 그 복제본을 사용한 결과가 아닙니다.
- 단일 구형 두부의 앞면과 양측면에 눈 둘·입·코·귀 둘, 총 여섯 `LOOP`를 입력 삼각 연결과 독립된 새 격자에 배치했습니다. 독립 삼각 입력 두 종에서 목표 5,000에 5,012쿼드, 최대 종횡비 4.85, 표본 표면 오차 최대 0.00696을 확인했고 격리 Blender worker와 정면·좌우 와이어를 검토했습니다. 실제 해부학 형태나 개구부는 만들지 않습니다.
- 앞선 단계에서 사각 축 정렬 단일 닫힌 T형 몸통·팔의 첫 연결형 격자를 두 입력으로 검증했습니다. 목표 900에 928쿼드, 최대 종횡비 1, 세 폐루프를 확인했습니다. 일반 캐릭터의 관절 전이는 여전히 미지원입니다.
- 분리된 메시 조각을 각각 지원되는 격자 경로로 처리하고 면적에 따라 쿼드 예산을 배분합니다. 가이드가 한 표면에 명확히 대응할 때만 적용하며 조각 사이 정점은 병합하지 않습니다. 독립 튜브와 큐브의 합성 입력은 목표 768에서 744쿼드였습니다. 이 분리 조각 경로는 연결된 몸통·팔이나 임모탈에 적용할 수 없습니다.
- 21,780면의 큰 삼각 구형 입력을 프록시 축소 없이 원본에서 직접 처리해 목표 5,000에 4,892쿼드를 확인했습니다.
- 닫힌 단일 구형 두부의 같은 큐브 면에 놓인 볼록한 눈·입 `LOOP` 세 개를 입력 삼각 연결과 독립된 새 쿼드 격자에 배치했습니다. 2,400 목표에서 2,314쿼드, 5,000 목표에서 4,892쿼드를 확인했습니다. 루프 안쪽은 면으로 채웁니다.
- 독립된 열린 삼각 튜브의 `LOOP` 관절 링과 `STRIP` 길이 방향 열을 입력 삼각 연결과 별도로 생성했습니다. 256 목표에서 256쿼드를 확인했습니다. 둥근 팔이 몸통과 연결되는 일반 분기는 아직 미지원입니다.
- 두 경로를 격리 Blender 5.2 worker로 검증하고 원본 보존을 확인했습니다. 실제 코·귀의 해부학 구조, 눈꺼풀·입술과 다중 조각 캐릭터의 자동 배치는 아직 미지원입니다.
- 삼각화된 규칙 격자의 닫힌 합성 얼굴에서 서로 분리된 폐루프 세 개를 확인하며 원래 쿼드 연결을 복원하는 제한적 경로를 추가했습니다. 일반 캐릭터의 새 얼굴·관절 루프 생성은 아직 미지원입니다.
- 격자 결과의 bowtie·가이드 엣지 경로와 양방향 표면 표본 오차를 검사하고, 종횡비 20 초과 결과를 적용하지 않도록 했습니다. 폐루프 판정에서 중심 정점을 여러 번 공유하는 잘못된 경로도 거부합니다.
- worker 입력의 큰 기하 배열을 버전·지문·크기·SHA-256 검사가 있는 바이너리 버퍼로 분리했습니다. 출력은 계속 JSON입니다.
- 비다양체 원본의 큰 메시 경로가 격자 배치 실패 뒤 임시 프록시 처리로 진행하도록 수정했습니다. 임모탈 입력에서 기존 결과의 최대 종횡비 3584.18을 확인했고 새 품질 검사에서는 결과 적용을 거부합니다.
- 루프·패치 배치와 정수 격자 기반의 새 엔진 설계, 데이터 계약, 구현 순서와 품질 합격 기준을 문서화했습니다.
- 연결된 공평면 사각 차트의 공유 정수 분할과 열린 원통형 튜브의 주기 링 격자를 추가했습니다. 큐브 384쿼드와 원통 128쿼드 구조를 격리 Blender에서 확인했습니다.
- `AUTO`·`STRUCTURED`·`LEGACY` 생성 방식을 추가했습니다. 미지원 형상의 실험 엔진 사용을 경고하고 필수 가이드를 보존할 수 없으면 중단합니다.
- 가이드의 `LOOP`·`STRIP`·`DIRECTION` 종류와 스플라인 닫힘 여부를 Blender 입력과 worker 계약에 전달합니다. 원통 단면의 LOOP와 길이 방향 STRIP을 실제 출력 링·엣지 열로 고정합니다.
- 입력 11,094면의 세분화 큐브를 프록시 축소 없이 격자 경로로 처리했습니다. 원본 면과 결과 격자를 검증했습니다.
- 기존 실험 엔진이 균일 격자·캐릭터 부위별 루프 요구를 충족하지 못한다는 점을 README와 검증 문서에 명시했습니다.
- 대칭 평면에 점으로만 닿는 조각을 잘못 용접해 bowtie 연결을 만들던 경우를 수정했습니다.
- 정렬 과정에서 고정된 조각에 불필요한 표면 매핑을 시도하던 오류와 작은 유효 면을 퇴화 면으로 오판정하던 면적 기준을 수정했습니다.
- 큰 입력 worker의 실행·취소·원본 보존 및 특징선 정점 보호 검사를 추가했습니다.
- 큰 입력에서 기존 엔진 상한을 넘는 경우만 격리된 background Blender worker로 우회해 임시 Decimate 프록시를 만들고, 자체 쿼드 엔진과 원본 BVH 기준 검증을 거쳐 결과를 적용하도록 수정했습니다.
- 최종 쿼드 결과는 자체 엔진이 만들지만 큰 입력 프록시 생성에는 Blender Decimate를 사용한다는 점을 명확히 했습니다.
- 목표 쿼드 수와 상용 품질은 보장 대상이 아니며, 실패·취소·입력 변경 시 원본을 보존하고 결과 적용을 중단한다는 설명을 보강했습니다.

## 0.3.0 — 2026-09-19

- 첫 공개 릴리스와 Blender Extensions 원격 저장소를 구성했습니다. 새 릴리스의 검증·ZIP 게시·Pages 업데이트 인덱스 생성을 자동화했습니다.
- 누적 QEM과 원본 표면 거리 검사를 적용한 엣지 축소·분할로 폴리곤 감소 및 목표 수 피드백 제어를 구현했습니다.
- 형상 편차가 크면 해상도를 보정하고 기준을 넘는 결과 적용을 거부합니다.
- 로컬 원점의 양의 영역을 기준으로 X/Y/Z 절단·미러·경계 연결을 구현했습니다.
- 특징선·곡률·가이드의 4방향장을 계산하고 패치 선택과 내부 정점 정렬에 적용합니다.
- 밀도 페인트를 실제 엔진에 전달하고, 원본 표면 재투영과 표면 편차·대칭 오차·방향 정렬 점수를 추가했습니다.
- 형상 제약으로 목표 개수에 도달하지 못한 경우에도 현재 면 수와 표면 편차 기준을 유지하며 밀도 재분배를 수행합니다.
- 고밀도 영역의 허용 표면 편차를 좁혀 세부 면을 보존하고, 거의 같은 축소 비용의 플랫폼별 정렬 차이를 줄였습니다.
- 위상과 특징선 제약으로 목표 수가 정확히 실현되지 않으면 실제 개수와 오차를 보고합니다.

## 0.2.0 — 2026-09-19

- 독립 Python 실험 엔진으로 특징선을 보존하는 쿼드 메시 생성 경로를 추가했습니다.
- 인접 삼각형의 형상과 가이드 방향을 평가하고 공유 엣지 중점과 면 중심으로 쿼드를 생성합니다.
- 목표 수에 가까운 균일 분할 단계 선택과 실제 쿼드 수·품질 보고를 추가했습니다.
- 성공한 결과를 별도 오브젝트로 만들며 실패·취소 시 원본 데이터를 보존합니다.
- 폴리곤 감소, 정확한 목표 개수, 강제 대칭, 적응형 밀도와 전역 엣지 흐름 최적화는 아직 지원하지 않습니다.

## 0.1.0 — 2026-09-19

- Blender Extension 개발 기반과 독립 엔진 입력 경계를 구성했습니다.
- 목표 쿼드 수, 대칭 축, 하드 엣지, 가이드 커브, 밀도 설정을 준비했습니다.
- 메시 분석과 밀도 속성 준비 기능을 추가했습니다.
- macOS와 Windows 전용 개발 프로필 실행기, 로컬 패키징 및 CI 검증 구성을 추가했습니다.
- 리토폴로지 엔진과 쿼드 메시 생성은 후속 구현 대상입니다.
